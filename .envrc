# Load .env file if it exists
dotenv_if_exists

# Watch additional files that might affect the environment
watch_file pyproject.toml
watch_file uv.lock

# Load the Nix flake environment
# This automatically watches flake.nix and flake.lock
use flake
